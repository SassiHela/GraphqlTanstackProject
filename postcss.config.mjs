const config = {
  theme: {
    extend: {
      fontFamily: {
        sans: ['Abel'], // Set Abel as the default sans-serif font
      },
    },
  },
  plugins: ["@tailwindcss/postcss"],
};

export default config;
